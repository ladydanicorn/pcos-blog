---
import '../styles/global.css';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog');
---

<html lang="en">
  <head>
    <title>PCOS Health & Fitness</title>
    <link rel="stylesheet" href="../styles/global.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="bg-rose-50 text-gray-800 font-sans">
    <header class="bg-white border-b border-rose-200 shadow-sm p-6 text-center">
      <h1 class="text-4xl font-bold text-rose-700 mb-2">PCOS Health & Fitness</h1>
      <p class="text-lg text-rose-500">Honest tips. Real food. Hormone-friendly living.</p>
    </header>

    <main class="max-w-4xl mx-auto px-4 py-10">
      <section class="mb-12 text-center">
        <h2 class="text-2xl font-semibold mb-4">Welcome!</h2>
        <p class="text-gray-700 leading-relaxed">
          This blog is for anyone navigating PCOS — whether you're newly diagnosed or just looking for sustainable ways to feel better. We're here to talk about food, movement, mood, and all the weird little things nobody else seems to mention.
        </p>
      </section>

      <section>
        <h2 class="text-2xl font-semibold mb-6 text-rose-700 text-center">Recent Posts</h2>
        <div class="grid sm:grid-cols-1 md:grid-cols-2 gap-6">
          {posts.slice(0, 4).map(post => (
            <a href={`/blog/${post.slug}/`} class="block rounded-xl overflow-hidden shadow-md bg-white border border-rose-100 hover:shadow-xl hover:border-rose-300 transition">
              {post.data.image && (
                <img src={post.data.image} alt={post.data.title} class="w-full h-48 object-cover" />
              )}
              <div class="p-6">
                <h3 class="text-xl font-semibold text-rose-600 mb-1">{post.data.title}</h3>
                <p class="text-sm text-gray-500 mb-2">{new Date(post.data.pubDate).toDateString()}</p>
                <p class="text-gray-700">{post.data.description}</p>
              </div>
            </a>
          ))}
        </div>
      </section>
      
      
    </main>

    <footer class="text-center text-sm text-gray-500 py-8">
      &copy; 2025 PCOS Health & Fitness — Built with 💖 by Danielle
    </footer>
  </body>
</html>
